import numpy as np

def sin (a, x):
    y = np.sin(a*x)

    sudut = float(input('masukan besar sudut istimewa: '))
    sine = math.radians(sudut)
    print (sine)

def cos ():
    sudut = float(input('masukan besar sudut istimewa: '))
    cosine = math.radians(sudut)
    print (cosine)

def tan ():
    sudut = float(input('masukan besar sudut istimewa: '))
    tangen = math.radians(sudut)
    print (tangen)


